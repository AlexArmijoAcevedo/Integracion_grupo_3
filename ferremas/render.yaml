services:
  - type: web
    name: ferremas
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn taller_ferremas.wsgi:application
    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: taller_ferremas.settings
      - key: DATABASE_URL
        value: sqlite:///db.sqlite3
